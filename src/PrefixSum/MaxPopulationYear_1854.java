package PrefixSum;

public class MaxPopulationYear_1854 {
    public int maximumPopulation(int[][] logs) {
        if(logs.length == 1) return logs[0][0];
        //first value
        int i=0;// to track
        int maxPopulation = 1;
        int currentPopulation = 1;
        int currentYear = logs[0][0];
        //
        for(int j=1;j<logs.length;j++){

        }


        return 0;
    }
}
